fun Bai07() {
    // While dung khi so lan lap lai phu thuoc vao mot dieu kien
    var number=10

    while (number>=1) {
        println(number)
        number--
    }

    // Repeat dung khi da chinh xac so lan lap lai
    repeat(5) {
        println("Hoc kotlin")
    }
}

Bai07()